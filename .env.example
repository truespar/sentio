# Copy to .env for local development. Gitignored.
#
# Only needed when regenerating the sqlx offline cache with
# `cargo sqlx prepare --workspace`; normal builds use the committed .sqlx/.
DATABASE_URL=postgres://sentio:sentio@localhost:5432/sentio
