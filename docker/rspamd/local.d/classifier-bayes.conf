# Sentio OSS quickstart - Bayes classifier backed by Redis.
# Training endpoints exposed via Sentio's own API at
# /v1/spam/learn-spam and /v1/spam/learn-ham.
backend = "redis";
autolearn = true;
min_tokens = 11;
new_schema = true;
expire = 8640000;   # 100 days
